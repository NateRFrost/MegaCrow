import type {
  CompletionItem,
  ElementCompletionContext,
} from "src/language-service/completion/types";

export const completeLocalizedInclude = (
  _ctx: ElementCompletionContext
): CompletionItem[] => [];
